apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.docker.name }}
  namespace: {{ .Values.namespace }}
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: {{ .Values.docker.dockerconfigjson | quote }}